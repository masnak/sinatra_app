require 'spec_helper'

describe Enpit::Birthday do
  it 'has a version number' do
    expect(Enpit::Birthday::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
